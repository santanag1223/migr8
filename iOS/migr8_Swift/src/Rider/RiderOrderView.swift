import SwiftUI
import MapKit

struct RiderOrderView: View {
    var rider: Rider
    @State private var selectedTab = 0
    var body: some View {
        // Display different orders user's can place
        // as well as a section for recent orders
        // and order history
        ZStack {}
    }
}

